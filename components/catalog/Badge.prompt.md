Small square uppercase label for sale flags, "New", and stock states.

```jsx
<Badge variant="sale">-20%</Badge>
<Badge variant="success">In stock</Badge>
```

Variants: `sale` (magenta), `accent` (yellow), `info` (blue), `success` (green), `neutral` (gray). Keep labels to one or two words.
