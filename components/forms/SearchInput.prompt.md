The big rounded-pill search field that anchors the ARMOR BIKE header.

```jsx
<SearchInput value={q} onChange={e => setQ(e.target.value)} />
```

Always full-width within the header's center column. Leading magnifier icon is built in. Use `size="lg"` on focused search/landing pages.
