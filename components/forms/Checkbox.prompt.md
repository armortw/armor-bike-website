Square checkbox used across the ARMOR BIKE facet/filter sidebar. Supports a trailing muted result count.

```jsx
<Checkbox label="In stock" count={75} checked={inStock} onChange={setInStock} />
```

The `count` prop right-aligns a muted number — the standard pattern for filter facets ("Sale %", "Disc Brake", etc.).
